Terraform :: Environments :: << ENV >> :: 050-DNS

This layers configures the Route53 DNS zones used by this environment.
It depends on the global dns layer to create the delegation set and root domain.
